# Sanity Configuration (Public - exposed to client)
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity Configuration (Private - server-only)
SANITY_API_TOKEN=your-sanity-token-for-write-operations
SANITY_API_VERSION=2023-05-03

# Optional: Sanity Studio URL for preview mode
SANITY_STUDIO_URL=http://localhost:3333

# On-Demand ISR Revalidation Secret
# Used by Sanity webhooks to trigger instant cache updates
# Generate with: openssl rand -base64 32
REVALIDATE_SECRET=your-random-secret-token

# Sanity Webhook Secret（成分更新時の自動再計算用）
# Generate with: openssl rand -base64 32
# Sanity管理画面でWebhook設定時に同じ値を設定:
# 1. sanity.io/manage > API > Webhooks > Add webhook
# 2. URL: https://suptia.com/api/webhooks/sanity/ingredient-update
# 3. Filter: _type == "ingredient"
# 4. Secret: この環境変数と同じ値
SANITY_WEBHOOK_SECRET=your-sanity-webhook-secret

# GitHub Token（Webhook経由でGitHub Actionsをトリガーする場合）
# Personal Access Token (classic) with 'repo' scope
# https://github.com/settings/tokens
# 注意: Vercel環境変数にも設定が必要
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# ============================================================================
# Supabase (Authentication & Database)
# ============================================================================
# Get credentials at: https://supabase.com/dashboard
# セットアップ手順:
# 1. Supabaseプロジェクトを作成
# 2. Project Settings > API からキーを取得
# 3. Authentication > Providers でOAuthを設定（Google等）

# Public keys (exposed to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key (server-only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Site base URL (production canonical)
NEXT_PUBLIC_SITE_URL=https://suptia.com
# (Legacy fallback; will be removed in future)
# NEXT_PUBLIC_BASE_URL=https://suptia.com

# Security Note:
# - NEXT_PUBLIC_* variables are exposed to the client bundle
# - Other variables are server-only and should contain sensitive data
# - Never put tokens or secrets in NEXT_PUBLIC_* variables

# ============================================================================
# EC Site API Integration (Phase 2.5+)
# ============================================================================

# ----------------------------------------------------------------------------
# 楽天市場API（優先 - MVP期間中のメインデータソース）
# ----------------------------------------------------------------------------
# Get credentials at: https://webservice.rakuten.co.jp/
# - 売上要件: なし
# - 無料枠: 月間10,000リクエスト
# - 推奨: MVP期間中はこちらのみ使用
RAKUTEN_APPLICATION_ID=your_rakuten_application_id
RAKUTEN_APPLICATION_SECRET=your_rakuten_application_secret
RAKUTEN_AFFILIATE_ID=your_rakuten_affiliate_id

# ----------------------------------------------------------------------------
# Yahoo!ショッピングAPI（フェーズ2.5 - 価格比較拡張）
# ----------------------------------------------------------------------------
# Get credentials at: https://e.developer.yahoo.co.jp/dashboard/
# - 売上要件: なし
# - 無料枠: 1リクエスト/秒
# - アフィリエイト: バリューコマース（報酬率1-50%）
YAHOO_SHOPPING_CLIENT_ID=your_yahoo_client_id

# ----------------------------------------------------------------------------
# バリューコマース（Yahoo!ショッピング アフィリエイト）
# ----------------------------------------------------------------------------
# Get credentials at: https://aff.valuecommerce.ne.jp/
# セットアップ手順:
# 1. バリューコマースに登録・承認
# 2. 「Yahoo!ショッピング」と提携申請
# 3. 管理画面でSIDとPIDを取得
# 詳細: docs/VALUE_COMMERCE_SETUP_GUIDE.md を参照
VALUE_COMMERCE_SID=s12345678
VALUE_COMMERCE_PID=p12345678

# ----------------------------------------------------------------------------
# Amazon Product Advertising API 5.0（オプション - 売上発生後に有効化）
# ----------------------------------------------------------------------------
# Get credentials at: https://affiliate.amazon.co.jp/
#
# ⚠️ 重要: Amazon PA-APIには売上要件があります
# - 過去30日以内に3件以上の売上が必要
# - 売上がない場合、APIアクセスが制限または停止されます
# - 新規アカウントは30-90日の猶予期間がある場合もあります
#
# 推奨戦略:
# 1. MVP期間中は ENABLE_AMAZON_API=false のまま（楽天のみ使用）
# 2. 初回売上達成後、ENABLE_AMAZON_API=true に変更
# 3. 継続的に売上を維持（月3件以上推奨）
#
# 📌 Suptia設定:
# - Store ID (Associate Tag): suptia69-22
# - 登録日: 2025-11-02
# - ステータス: 180日間の認証期間中
#
ENABLE_AMAZON_API=false  # 売上発生後にtrueに変更
AMAZON_ACCESS_KEY_ID=your_amazon_access_key_id
AMAZON_SECRET_ACCESS_KEY=your_amazon_secret_access_key
AMAZON_ASSOCIATE_TAG=suptia69-22  # Suptia Store ID
AMAZON_REGION=us-west-2
AMAZON_MARKETPLACE=www.amazon.co.jp

# 将来のフェーズで追加予定:
# - アフィリエイトネットワークAPI (CJ, Impact, Rakuten Advertising)
# - スクレイピングサービス (Bright Data)
# - キャッシュストレージ (Vercel KV Store)

# ============================================================================
# Contact Form & Email (Phase 1)
# ============================================================================

# ----------------------------------------------------------------------------
# Resend API（お問い合わせフォームのメール送信）
# ----------------------------------------------------------------------------
# Get API key at: https://resend.com/api-keys
# - 無料枠: 月100通、1日3,000通
# - 独自ドメイン設定が推奨（本番環境）
# - 開発環境では onboarding@resend.dev が使用可能
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# お問い合わせ受信メールアドレス
# デフォルト: info@suptia.com
CONTACT_EMAIL=info@suptia.com

# ----------------------------------------------------------------------------
# Vercel Cron Secret（価格アラート等のCronジョブ認証用）
# ----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
# Vercelダッシュボードの環境変数に設定
CRON_SECRET=your-cron-secret-here

# ----------------------------------------------------------------------------
# Batch Sync Token（バッチ価格同期API認証用）
# ----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
# Cron JobからのAPI呼び出し時の認証に使用
BATCH_SYNC_TOKEN=your-batch-sync-token-here

# ============================================================================
# Analytics & SEO (Phase 3)
# ============================================================================

# ----------------------------------------------------------------------------
# Google Analytics 4
# ----------------------------------------------------------------------------
# Get credentials at: https://analytics.google.com/
# セットアップ手順:
# 1. Google Analyticsにログイン
# 2. 新しいプロパティを作成（GA4を選択）
# 3. データストリームを作成（ウェブを選択）
# 4. 測定IDをコピー（G-XXXXXXXXXX形式）
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ----------------------------------------------------------------------------
# Google Search Console
# ----------------------------------------------------------------------------
# Get credentials at: https://search.google.com/search-console
# セットアップ手順:
# 1. Search Consoleにログイン
# 2. プロパティを追加（URLプレフィックスを選択）
# 3. 所有権の確認方法を選択:
#    - HTMLファイル: public/google-verification-code.html
#    - DNSレコード: TXTレコードをDNSに追加
#    - Googleタグマネージャー: GTMコンテナIDを使用
# 4. サイトマップを送信: https://suptia.com/sitemap.xml
GOOGLE_SEARCH_CONSOLE_VERIFICATION=your-verification-code

# Google Search Console API (MCP + Next.js統合用)
# セットアップ手順: .mcp/QUICKSTART.md 参照
# OAuth認証ヘルパー: node scripts/generate-google-auth-url.mjs
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret
GOOGLE_REFRESH_TOKEN=1//your-google-refresh-token

# ----------------------------------------------------------------------------
# Vercel Analytics
# ----------------------------------------------------------------------------
# Vercelダッシュボードで有効化
# https://vercel.com/docs/analytics
# 自動的に統合されるため、環境変数は不要
# Speed Insights と Web Vitals の自動トラッキング

# ============================================================================
# AI & Image Generation
# ============================================================================

# ----------------------------------------------------------------------------
# Anthropic Claude API（Instagram投稿生成等）
# ----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# ----------------------------------------------------------------------------
# Google AI API（Gemini等）
# ----------------------------------------------------------------------------
GOOGLE_AI_API_KEY=AIzaxxxxxxxxxxxxxxxxx

# ----------------------------------------------------------------------------
# Cloudinary（画像生成・ストレージ）
# ----------------------------------------------------------------------------
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================================================
# SNS Integration
# ============================================================================

# ----------------------------------------------------------------------------
# Threads API
# ----------------------------------------------------------------------------
THREADS_USER_ID=your-threads-user-id
THREADS_ACCESS_TOKEN=your-threads-access-token

# ----------------------------------------------------------------------------
# X (Twitter) API
# ----------------------------------------------------------------------------
X_API_KEY=your-x-api-key
X_API_KEY_SECRET=your-x-api-key-secret
X_ACCESS_TOKEN=your-x-access-token
X_ACCESS_TOKEN_SECRET=your-x-access-token-secret
